# 仓库 Agent 指令

[English](AGENTS.md)

## 指令范围

- 本文件包含整个仓库共享的规则。
- 修改某个子树前，检查是否存在更近的 `AGENTS.md`；本地指令会补充或覆盖本文件。
- 本仓库只使用 `AGENTS.md` 存放 Agent 指令，使用 `CONTEXT.md` 存放领域上下文。不要在工具专用指令文件中重复这些规则。
- 每个独立管理的大型模块只保留一份 `AGENTS.md` 和一份 `CONTEXT.md`。除非某个更细的子树确实具有独立所有权和规则，否则不要增加更细粒度的文件。
- 无法自动发现 `AGENTS.md` 的 Agent 宿主必须通过配置或提示读取适用文件。
- 发现与优先级规则参见 [`docs/agents/instruction-files.md`](docs/agents/instruction-files.md)。

## 领域文档

- 修改领域行为或术语前，先阅读根目录 [`CONTEXT.md`](CONTEXT.md)，再阅读相关模块的 `CONTEXT.md` 和 ADR。
- `CONTEXT.md` 文件只用作词汇表。实现约束放入 `AGENTS.md`，持久架构决策放入 ADR。
- 所有 ADR 都位于根目录 [`docs/adr/`](docs/adr/)，包括只适用于单个模块的决策。
- 参见 [`docs/agents/domain.md`](docs/agents/domain.md)。

## 代码设计与风格

### 最小实现

- 只实现当前已确认的需求。不要为假设的未来用途增加字段、API、类、配置、状态或兼容路径。
- 除非某个抽象能消除真实重复、显著降低复杂度或落实已建立的模块边界，否则不要引入它。
- 当现有组件、框架 API 或模块能力已经拥有所需行为时，应复用它。
- 将修改限制在请求的行为范围内。不要在功能工作中混入无关重构。

### 替换与清理

- 当新实现替代旧实现时，删除旧类、方法、组件、路由、类型、配置和测试。没有明确兼容要求时，不要并行维护多个实现。
- 重命名一个概念时，更新其文件名、导出、引用、路由、类型、消息、测试和生成的声明。不要混用新旧术语。
- 完成任务前，删除临时脚本、诊断代码、过渡配置、被注释掉的代码和未使用实现。
- 除非当前兼容要求明确需要，否则不要保留别名、双格式读取器或旧入口。
- 删除或重命名代码后，搜索所有引用并确认不存在过期路径。

### 单一事实来源

- 每个概念只能有一个规范名称、一个权威类型或定义、一个配置来源和一条主要执行路径。
- 在所属边界集中管理常量、枚举、默认值、支持值集合和共享规则；其他位置通过导入使用，不要重复声明。
- 将状态存放在最接近事实来源的位置。没有同步要求时，不要在组件、Store、路由、缓存或持久层之间镜像同一状态。
- 不要在源代码、环境文件、启动脚本、IDE 设置和文档之间重复配置。
- 在适当的输出边界派生展示数据，不要将它与规范源值并列持久化。

### 框架与 API 使用

- 使用框架支持的生命周期、组件契约和扩展点，不要手工重建已有工作流。
- 用受支持的替代方案替换已弃用 API。不要压制警告、包装已弃用 API，或使用类型断言保留过时调用方式。
- 在已弃用工作流的所属边界修复问题，不要只改发出警告的那一行。
- 包装第三方组件时，保留包装器未明确覆盖的所有属性、特性、事件和插槽。
- 包装器只能更改它有意负责的行为，不得意外缩窄底层组件的能力。

### 类型

- 在组件、组合式函数、表单、服务或 API 初始化边界声明类型，让调用自行推断这些类型。
- 当所属 API 可以一次完成类型定义时，不要在调用处反复用泛型覆盖返回类型。
- 避免使用 `any`、双重断言、无充分理由的非空断言，以及其他掩盖模型缺陷的类型逃逸。
- 相比重复类型声明，优先使用类型推断、受约束泛型、可辨识联合和标准工具类型。
- 按内聚资源组织 API 类型，并用 `Pick`、`Omit`、`Partial` 等工具派生操作输入，不要为每个操作创建近乎相同的接口。
- 类型名称必须表达领域含义，不要重复底层语言类型已经明显表达的信息。

### 命名

- 根据职责或行为命名类、方法、变量和组件，不要根据偶然的实现细节命名。
- 操作使用清晰动词，例如 `validate`、`create`、`remove` 和 `resolve`。
- 当参数或返回类型已经说明类型时，名称中避免冗余的类型词。
- 项目自有公共接口优先使用稳定的领域或协议术语，不要使用供应商、框架或基础设施品牌名称。
- 只有仓库已经采用相同规范形式时才使用缩写。

### 模块边界

- 将代码放在拥有该职责的模块中；不要仅为调用方方便就在模块间复制状态或实现。
- UI 入口应专注于交互和展示。可复用业务规则放在所属服务或领域边界。
- 控制器和页面组件应保持轻量，只负责解析或展示数据，并委托实质工作流。
- 通用工具不得依赖具体页面、路由、Store 或 UI 组件。
- 模块之间通过明确的公共接口协作，不要导入其他模块的内部实现。
- 行为相似本身不足以建立共享依赖。只提取稳定且真正独立于调用方的行为。

### 控制流与错误

- 异步工作优先使用清晰的 `async`/`await` 控制流。
- 当清理必须在所有结果下执行时，使用 `finally` 恢复加载、锁定及其他临时状态。
- 只捕获当前层能够处理的错误。需要更高层决策的错误应继续传播。
- 不要使用空 `catch`、静默回退或掩盖失败的默认成功行为。
- 使用所属框架的校验、提交、持久化和生命周期流程，不要在每个调用方手工组合重复步骤。
- 在拥有数据一致性的层处理事务边界、幂等性和失败顺序。

### 前端代码

- Vue 组件使用 `<script setup lang="ts">`，并让类型、状态、计算值、处理函数和组件 API 设置保持有序且易于浏览。
- 手工定义的 CSS 类使用 BEM。Sass 嵌套可使用 `&__element` 和 `&--modifier` 表达关系。
- camelCase 只用于 TypeScript 和 JavaScript 标识符，不要将其作为另一种 CSS 类命名约定。
- 仅当不存在有意义的语义类时，才用工具类处理一次性布局或尺寸。可复用结构、状态和响应式行为使用语义化组件样式。
- 为纯图标控件和自定义交互元素提供无障碍名称；原生语义未提供键盘行为时，应自行提供。
- 面向用户的文本放在所属本地化资源中，不要把硬编码消息散落在组件里。

### 后端代码

- Spring Bean 使用构造函数注入和 `private final` 依赖。
- 控制器仅处理传输层关注点、请求解析、服务委托和响应构造。
- 业务校验、状态转换、事务和外部资源协调放在所属服务中。
- 相关外部配置通过有类型的配置对象绑定，不要在业务代码中零散读取环境值。
- 优先使用框架服务层持久化 API；只有服务 API 无法表达所需操作时才下沉到 Mapper 或自定义查询。
- 显式处理时间、时区和序列化行为，不依赖机器默认值。
- 返回稳定且适合客户端的业务错误，不要暴露内部异常详情。

### 注释与文档

- 注释只说明代码本身无法体现的约束、原因或权衡。不要复述直白语句。
- 不要在注释中保留已删除的实现。
- 执行规则放入 `AGENTS.md`，领域术语放入 `CONTEXT.md`，持久架构决策放入 ADR。
- 不要把 Agent、编辑器、IDE 或本地工作流要求实现成应用运行时行为。

### 完成标准

- 增加新实现前，检查相邻代码并搜索已有能力。
- 替换或重命名代码后，确认完整的旧引用链已经移除。
- 根据改动范围运行所属模块的格式化器、静态分析、类型检查和测试。
- 不要通过禁用规则、忽略错误或扩大类型逃逸来让检查通过。
- 确保最终差异中没有无关重构、临时产物、生成噪声或意外的配置回退。

## Issue 跟踪

- Issue 和 PRD 通过 `gh` 在 GitHub Issues 中跟踪。参见 [`docs/agents/issue-tracker.md`](docs/agents/issue-tracker.md)。
- 使用 [`docs/agents/triage-labels.md`](docs/agents/triage-labels.md) 中记录的规范分流标签。

## 测试

- 编写、修改、审查或运行测试前，阅读 [`docs/test/testing-guide.md`](docs/test/testing-guide.md)。
- 测试放在所属模块的 `src/test` 下；不要使用 `src/intg-test`。
- 使用 `mvn -f apps/server/pom.xml test` 运行快速后端测试。
- 使用 `mvn -f apps/server/pom.xml verify` 运行完整后端测试套件。它要求 Docker 提供 Testcontainers MySQL 8 和 Redis 8；不要替换成 H2、本地服务或共享服务。

## Agent 开发端口

- AI Agent 在开发环境启动 Admin 或 Mobile 时，必须使用所属应用 `.env.development` 中当前配置的端口：Admin 使用 `VITE_PORT`，Mobile 使用 `VITE_APP_PORT`。使用正常开发命令启动，以便 Vite 加载该文件；不要硬编码当前值，也不要通过命令行覆盖端口。
- AI Agent 启动后端时，必须使用 `apps/server/novum-bootstrap/src/main/resources/application.yml` 中当前的 `server.port` 值；不要硬编码或在启动命令中覆盖该值。
- 启动上述任一服务前，AI Agent 必须立即读取当前配置端口、检查该精确端口，并自动终止正在监听它的进程。不得仅为避开端口冲突而选择其他端口或修改应用配置。

## Git 与交付

- 除非用户明确要求提交，否则保持改动未提交。
- 用户要求提交时，使用 `pnpm run commit`；绝不直接调用 `git commit`。
- 除非用户明确要求远程交付，否则不要推送，也不要创建或更新 Pull Request。
- 远程交付必须通过 Pull Request。绝不直接推送到 `main` 或其他目标分支；除非用户另有指定，否则 PR 默认以 `main` 为基准分支。
